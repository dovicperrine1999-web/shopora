# Shopora Super Admin

Secure starter implementation for a single permanent super-admin account:

`ludovicperrine@icloud.com`

The password remains under Supabase Auth control. Forgot-password recovery uses the normal email reset flow. No default password is embedded.

Run `sql/01_super_admin.sql` in Supabase after the account exists.
